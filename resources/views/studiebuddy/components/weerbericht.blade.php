@foreach ($weerbericht as $weer)
    <p>{{$weer}}</p>
@endforeach

@foreach ($hours as $hour)
    <p>{{$hour}}</p>
@endforeach

@foreach ($temps as $temp)
    <p>{{$temp}}</p>
@endforeach

@foreach ($planning as $plan)
    <p>{{$plan}}</p>
@endforeach